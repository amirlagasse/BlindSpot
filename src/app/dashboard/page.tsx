import { Dashboard } from '@/components/dashboard/Dashboard';

export const metadata = {
  title: 'Your exposure | Blind Spot',
};

export default function DashboardPage() {
  return <Dashboard />;
}
